/**
 * Tham chiếu UI Tablet (web prototype) — sẽ xây lại thành Android app.
 *
 * Không mount vào router webapp Quản lý.
 * Shared widgets còn ở `src/components/` (QrScanner, ChecklistQuestion, …).
 *
 * Luồng dự kiến Android:
 * Login tabletX → chọn Zone → Dashboard máy → Checklist OK/NG → Socket chat
 */
export {};
